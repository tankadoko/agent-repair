For session repair requests, use `.agents/skills/repair-session/SKILL.md` and continue through application unless the user asks for a preview.

Keep personal sessions, configuration and experiment data outside this repository. Test development changes on temporary fixtures or copies of skills. Run `python -m unittest discover -s tests -v` after changing the harness.
